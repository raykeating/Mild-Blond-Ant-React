name: Ping my computer on code push

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Send HTTP request
      run: |
        curl http://184.148.107.164:7000
